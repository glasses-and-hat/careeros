import{Page}from'@/components/page';import{Button,Badge}from'@/components/ui';import{useUIStore}from'@/stores/ui-store';import{Sun,Moon,Monitor}from'lucide-react';
export default function SettingsPage(){const{theme,setTheme}=useUIStore();return <Page title="Settings" description="Personalize your CareerOS experience"><div className="max-w-3xl space-y-4"><section className="panel p-5"><h2 className="font-semibold">Appearance</h2><p className="mt-1 text-xs muted">Choose how CareerOS looks on this device.</p><div className="mt-4 grid grid-cols-3 gap-2">{([['light',Sun],['dark',Moon],['system',Monitor]]as const).map(([v,Icon])=><Button key={v} className={theme===v?'border-indigo-500 bg-indigo-500/10':''} onClick={()=>setTheme(v)}><Icon className="size-4"/>{v[0].toUpperCase()+v.slice(1)}</Button>)}</div></section><section className="panel p-5"><h2 className="font-semibold">Keyboard shortcuts</h2><div className="mt-4 divide-y">{[['Open command palette','⌘ K'],['Go to dashboard','G then D'],['Go to jobs','G then J'],['Go to applications','G then A']].map(([a,k])=><div className="flex items-center py-2 text-sm" key={a}><span>{a}</span><Badge className="ml-auto font-mono">{k}</Badge></div>)}</div></section><section className="panel p-5"><h2 className="font-semibold">API connection</h2><p className="mt-1 text-xs muted">The frontend uses an OpenAPI-generated client through the local API proxy.</p><Badge className="mt-3 border-emerald-500/30 text-emerald-600">Configured</Badge></section></div></Page>}
