package com.careeros.documents.infrastructure;import org.springframework.boot.context.properties.ConfigurationProperties;import java.time.Duration;@ConfigurationProperties("careeros.resume")public record ResumeDocumentProperties(String masterPath,String outputDirectory,String libreOfficePath,String pythonPath,Duration processTimeout){public ResumeDocumentProperties{if(outputDirectory==null)outputDirectory="./data/resumes";if(pythonPath==null)pythonPath="python3";if(processTimeout==null)processTimeout=Duration.ofSeconds(60);}}
