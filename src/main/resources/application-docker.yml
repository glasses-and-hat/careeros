spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/${DB_NAME:careeros}

logging:
  level:
    com.careeros: INFO

careeros:
  aggregators:
    builtin-chicago:
      enabled: ${BUILTIN_CHICAGO_ENABLED:true}
